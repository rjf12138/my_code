{
    "tasks": [
        {
            "type": "shell",
            "label": "my_code",
            "command": "${workspaceFolder}/compile.sh",
            "args": [
                "-r"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}